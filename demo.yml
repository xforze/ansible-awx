- name Hello Ping!
  hosts: all
  become: yes
  tasks:
    - name: Hello World!
      shell: echo "Hi! Tower is working."
    - name Ping only
      ansible.builtin.ping:
    - name debug task
      debug:
        msg: "{{ ansible_facts}}"
